/**
 * DEPRECATED: See Phase0HarnessManualSave.test.ts instead.
 *
 * The .tsx version required jsdom environment which had compatibility issues
 * with fake-indexeddb. The test has been moved to .test.ts (node environment)
 * where all dependencies are properly available.
 */
