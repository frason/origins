Parent: #159

## Status (pass 87 rescope — supersedes pass 85 rescope)
Pass 85's 4-gap rescope is now down to **2 remaining sub-gaps plus a commit step**, per karen's
latest FAILED verdict (2026-09-07T05:24:35Z). Confirmed already implemented and passing tests —
**do not re-touch**: journal persistence wired into `App.tsx` (`loadFieldJournal`/
`saveFieldJournal` now called on every reset/load/new-world path), and `boundJournalStorage()`
now called from inside `addJournalEntry` (`fieldJournal.ts:347`) so storage is genuinely bounded.
`createRangeChangeEntry` exists and is wired into `recordJournalEntries`
(`fieldJournalIntegration.ts:198-241`) — the function itself is correct, only its trigger
condition has a bug (below).

**None of this is committed yet** — `git status` shows `App.tsx`, `fieldJournal.ts`,
`fieldJournalIntegration.ts`, `store.ts`, `FieldJournal.tsx` all as uncommitted working-tree
modifications on top of the last real commit (`442f3e3`, an unrelated issue). Commit as soon as
the 2 gaps below are fixed and tests pass — this has caused repeated crash-before-commit rounds
on this issue already (~10+ rounds total).

## Scope (narrowed — only these 2 items, then commit)
1. **Range-change false positive on first tick.** The "meaningful change" check in
   `fieldJournalIntegration.ts` (`percentChange = previousCells.length > 0 ? cellDifference /
   previousCells.length : 1`) treats a lineage's very first tracked tick (no prior cells) as a
   100% range change, firing a spurious range-change entry alongside every first-sighting entry.
   Fix: skip emitting a range-change entry when a lineage key has no prior entry in
   `tracker.occupiedCells` at all (i.e. this is its first-sighting tick), not just when
   `previousCells.length > 0` inside the percent-change math. Re-run
   `npx vitest run src/tests/fieldJournalIntegration.test.ts` and confirm "should reconstruct
   full lineage history from journal entries" passes again (currently gets 2 entries for lineage
   A instead of the expected 1).
2. **Wire navigation callbacks + add missing test file.** `FieldJournal.tsx` already has
   `onNavigateToTile`/`onNavigateToLineage` props with click handlers (lines 134-167, 218-235,
   319-323) but `App.tsx` never passes them — only `onNavigateToTick` is supplied
   (`App.tsx:658`). Add real `onNavigateToTile` (pan/jump the world view to that grid cell) and
   `onNavigateToLineage` (filter/jump to the parent lineage's entries) implementations in
   `App.tsx`, mirroring how `navigateToJournalTick` is already wired. Add the missing dedicated
   test file (e.g. `src/tests/FieldJournal.test.tsx`) covering both click paths — none currently
   exists (`fieldJournal.test.ts`/`fieldJournalIntegration.test.ts`/
   `fieldJournalPersistence.test.ts` all test non-UI logic only).
3. **Commit.** Once 1 and 2 pass their tests, commit all 5 changed files
   (`App.tsx`, `fieldJournal.ts`, `fieldJournalIntegration.ts`, `store.ts`, `FieldJournal.tsx`).

## Done when
- `fieldJournalIntegration.test.ts` passes with the reconstruct-lineage-history test back to
  expecting exactly 1 entry for a lineage's first-sighting tick.
- Clicking a journal entry's map-region link pans/jumps the world view there; clicking a
  speciation entry's ancestral-lineage link jumps to the parent lineage's entries.
- A new FieldJournal.tsx test file covers both navigation click paths.
- `npx tsc --noEmit` clean on the files this issue touches (repo-wide tsc has known pre-existing
  errors in unrelated save-slot/decomposition test files — not in scope here).
- Work is committed.

## Output
Write a concise summary (≤40 lines) to state/worker_output.txt.

<!-- agent-planned -->
