Parent: #159

## Status (pass 87 rescope — supersedes pass 84 rescope)
Pass 84's 4-item rescope is now down to **2 remaining sub-gaps**, per karen's latest FAILED
verdict (2026-09-07T03:44:55Z, commit `442f3e3`). Confirmed already PASSING and committed —
**do not re-touch**: environmental-shock emission (engine.ts:1000-1018), region on
birth/death/mutation/intervention events, mobile drag-to-pan (`onPointerMove` in
EvolutionTimeline.tsx:163-176), and the 35 existing tests in
`src/tests/evolutionTimelineModel.test.ts`.

## Scope (narrowed — only these 2 items)
1. **Region on speciation/extinction events.** In `src/simulation/engine.ts`, the speciation
   event push (~line 888-895) and extinction event push (~line 1046-1053) still carry no
   `affectedRegion`/x/y — the speciation branch likely needs an x/y threaded through
   `IncipientSpecies`/`establishableCandidates` (the founding candidate currently has no
   coordinates); extinction can use the species' last known death location. Then in
   `src/ui/evolutionTimelineModel.ts:161-166`, call `getGridRegion()` for the `speciation` and
   `extinction` branches of `createEventPin`, mirroring the existing birth/death/mutation
   pattern, so the region filter works for all 7 pin types instead of 5.
2. **Test coverage gaps.** Zero test coverage currently exists anywhere in the repo for the
   mobile pointer drag-to-pan interaction (`grep -rn "onPointerMove|EvolutionTimeline"
   src/tests/` only matches unrelated `buildEvolutionTimeline` imports). Add a test (new file,
   e.g. `src/tests/EvolutionTimeline.pointer.test.tsx` using React Testing Library +
   `fireEvent.pointerDown/Move/Up`, or extract the pan-math into a pure/testable function) that
   exercises drag-to-pan: simulate pointerdown + pointermove while zoomed, assert `panX`
   changes and stays clamped. Also add tests for region assignment on speciation/extinction pins
   once item 1 lands, matching the existing birth/death/mutation test pattern.

## Done when
- Region filter works across all 7 event/pin types (birth, death, mutation, intervention,
  environmental-shock, speciation, extinction) — not just the 5 that already pass.
- A real test exercises the mobile drag-to-pan behavior (not just that handlers exist).
- New tests for speciation/extinction region assignment pass.
- `npx tsc --noEmit` clean on the files this issue touches (repo-wide tsc has known pre-existing
  errors in unrelated save-slot/decomposition test files — not in scope here).
- Work is committed.

## Output
Write a concise summary (≤40 lines) to state/worker_output.txt.

<!-- agent-planned -->
