<!-- BUDGET:START (auto-generated by budget_check.sh; do not edit by hand) -->
## 5h rolling budget — updated 03:10
Spent: $2.4 / $7.5 soft cap  ████░░░░░░░░░░ 32%
Runs: karen 2 · lead 1 · worker 2
<!-- BUDGET:END -->

# Project Status

_Updated by: Lead agent_ &nbsp;·&nbsp; _When: 2026-08-13 (planning pass #12)_

## Phase

**build — active pivot.** MVP core build (Phases 1–6) shipped and closed long ago; the project
then ran a long post-MVP feedback/beta-hardening loop (dozens of client-filed bugs/features, all
closed). On 2026-08-11 the client approved a **Crisis-Response Reframe pivot**: the game becomes
an active crisis-response loop (remote alien operator directing a scout + interventions on a
resource-scarce planet) instead of a passive ecosystem viewer. Full rationale lives in
`SPEC.md` → "Pivot: Crisis-Response Reframe" (source plan: `swift-hopping-codd.md`, client-side).
We are mid **Phase 0** — a cheap vertical slice proving the loop before Phases A–E.

## New this pass

**#227 finally succeeded after the outage-driven crash streak, but landed roughly — moved to
`agent-review` on a `MAX-TURNS-EXHAUSTED` run, and its expected output artifact is missing.**
`logs/dispatcher.log` shows #227 crashed 8 more times through 21:20 (continuing the pattern
flagged last pass), then a 9th attempt at 22:10-22:21 ran to the worker's turn limit
(`MAX-TURNS-EXHAUSTED ... cost=$0.23 ... treating as complete`) and the dispatcher moved it to
`agent-review` anyway per its "treat max-turns-exhausted as complete, verify before trusting"
policy. Checked directly: `git diff --stat` confirms real, substantial uncommitted changes to
`src/simulation/pivot/scout.ts` (+110/-lines) and `src/tests/scout.test.ts` (+317 lines) — so
the run did produce plausible battery/discovery/sync work, this isn't a `$0`-cost no-op. However,
the issue's required output artifact (`state/worker_output_227.txt`, per the issue's "Output"
section) **does not exist** — `find . -iname "*227*"` turns up nothing, and the general
`state/worker_output.txt` file still holds stale content from an unrelated, much older issue
(#101). This is exactly the kind of run karen's normal verification pass should scrutinize
harder than usual (incomplete self-report, non-clean completion signal) — flagging it here so
the next pass's read of the karen verdict isn't a surprise either way. No lead action taken on
#227 itself: it's correctly sitting in `agent-review`, RULE 1 will run karen on it automatically
next tick, and intervening to second-guess karen's job is outside my role. Board confirmed:
#229/#230/#237/#238 remain plain `agent-todo`, unaffected — with #227 out of the todo pool the
oldest-number starvation from last pass's window has already resolved itself (#229 is now oldest
and will be dispatched next), so #238's fairness fix is no longer urgent, just still good
hygiene to land eventually.

No inbox items, no untriaged issues, no `agent-triage` issues, no open `agent-question` issues
this pass — nothing to triage or unblock beyond the routine checks below. #237 has **still not
closed**, so per last pass's explicit instruction #228 correctly stays `agent-blocked` this pass
too (verified directly via `gh issue view`) — do not relabel back to `agent-review` yet. Checked
all `agent-backlog` issues' dependency chains directly: none have all deps closed yet (#231 still
needs #228 + #229; #232 still needs #227 + #228 + #229 + #230 + #231; #233 needs #232; the 30
paused pre-pivot issues all need #233) — no backlog promotions this pass.

**Prior pass's diagnosis (superseded above, kept for continuity):**

**Confirmed last pass's livelock fix worked, then found and filed a second, related dispatcher
fairness gap (#238) — not yet urgent, but real.** Checked `logs/dispatcher.log` per last pass's
"Up next" item #1: the karen re-run bleed on #228 is confirmed stopped — zero `VERIFY issue
#228` lines after 18:25 (when #228 was pulled to `agent-blocked`), exactly as designed. However,
the log also shows a **different, previously-undiscovered starvation pattern**: from 19:10 to
21:20 (2h10m, 7 consecutive worker-dispatch ticks), every single attempt picked issue #227 (the
lowest-numbered ready `agent-todo` issue) and crashed with `claude exited non-zero`, `$0` cost,
`duration_api_ms: 0` — the same outage signature already seen affecting karen (16:10-16:20) and
lead (19:00, 19:30) in this same window, strongly suggesting a transient upstream API outage
rather than anything wrong with #227's content. Read `scripts/dispatcher.sh` directly to confirm
why this stalled the whole queue rather than just #227: RULE 3's worker-crash branch (~line
718-726) relabels the crashed issue back to `agent-todo` but — unlike the karen-FAIL branch
(~line 560-569), which deliberately adds a FAILed issue to `state/cooldown.json` for exactly one
pass so it can't re-win oldest-number selection immediately — the crash branch never touches
`cooldown.json`. So a crash-cycled issue re-wins oldest-number selection on literally every
subsequent tick, meaning #229/#230/#237 (all independent, ready, unrelated to #227) got **zero**
dispatch attempts for over two hours while the outage lasted. This is bounded (not a true
infinite livelock like #228/#237 was) — `max_worker_attempts` is 20 and #227 had only used 7 by
the time this pass ran, so the existing auto-block-after-N-attempts logic (~line 638) would
eventually have kicked in — but it's a real gap that could stall the board for hours during any
future outage. Filed **#238** (`agent-todo`, no deps, ready now): extend the worker-crash branch
to add the crashed issue to `cooldown.json` using the exact same pattern the karen-FAIL branch
already uses. Did **not** manually intervene on #227 itself this pass (unlike #228 last pass) —
there's no evidence of a content problem, the outage signature matches system-wide failures seen
across all three agent types in the same window (not #227-specific), and the current lead pass
itself is running successfully, suggesting the outage has already cleared. Board confirmed
current: #227/#229/#230/#237 all still sit in plain `agent-todo` (7/20 attempts used on #227,
well under the auto-block threshold), #228 still correctly `agent-blocked` (since #237 has not
closed yet — no action needed there this pass, per last pass's explicit instruction not to skip
that check). No untriaged issues, no `agent-triage` issues, no open `agent-question` issues,
empty inbox this pass.

**Prior pass's diagnosis (superseded above, kept for continuity — still accurate as far as it
went, just didn't catch the scheduling deadlock):**

**Root-caused and broke a genuine dispatcher livelock (not just a slow bug) — this was the real
driver of the budget crisis, and it would have run forever unfixed.** Since the prior pass filed
#237, karen re-ran on #228 **five more times** (23:05, 00:23, 00:43, 01:04, 01:25), PASSING the
content every single time (work has been done and committed as `53ceeea` since ~21:48) and
failing only the known commit-gate bug each time — 5 wasted karen runs at $0.33–$0.79 each, zero
progress, entirely explaining why the 5h budget hit 93% of the $7.50 soft cap this pass. Read
`scripts/dispatcher.sh` directly to find out why #237 was never picked up despite sitting ready
in `agent-todo` the whole time: **RULE 1 unconditionally re-runs karen on the oldest
`agent-review` issue every tick, before RULE 3 ever reaches worker dispatch on `agent-todo`.**
Because #228's only fix (#237) can only be delivered by a *worker*, and #228 occupying
`agent-review` blocks all worker dispatch, the two issues were in a hard self-perpetuating
livelock — not merely slow, structurally permanent until someone intervened. Per my role
(plan/sequence via labels, never touch code), I broke the loop directly this pass: relabeled
**#228 from `agent-review` → `agent-blocked`** (the label the dispatcher itself defines for
"needs manual/lead intervention" — semantically exact fit) with an explanatory comment, and
confirmed via `gh issue list --label agent-review` that the board is now empty of review items.
The dispatcher's very next tick will fall through to RULE 3 and dispatch a worker on the oldest
`agent-todo` issue (#227, then #229, #230, #237 in number order) — no more karen re-runs will
fire until something is back in `agent-review`, so the budget bleed stops immediately.
**Action required next pass:** once #237 closes, relabel #228 back `agent-blocked` →
`agent-review` so the dispatcher's normal karen→commit-gate path re-verifies (should PASS
immediately, 6th time) and pushes/closes automatically — content is not in question, only the
gate. Flagging this prominently in "Up next" below so it isn't missed.

**Prior pass's diagnosis (superseded above, kept for continuity — still accurate as far as it
went, just didn't catch the scheduling deadlock):** Found and filed #228 is stuck in an infinite karen re-verification loop caused by a dispatcher
bug, not incomplete work. `logs/dispatcher.log` shows karen PASSED #228 twice (16:05, 17:23)
but both times logged "PASSED but commit gate failed — left in review", and the dispatcher then
re-runs the *entire* karen verification again every ~10-minute tick with #228 still in
`agent-review` — this is very likely the main driver of the 5h budget sitting at 90% of the
$7.50 soft cap (karen runs cost $0.30–$0.79 each, and it will keep re-firing forever unless
fixed). Root-caused directly (not just from logs): the worker for #228 committed its fix locally
as `53ceeea` ("Implement Phase 0: Localized Toxicity Spike crisis detection and Tier 1 response")
instead of leaving files uncommitted with a `Changed files:` manifest line in
`state/worker_output_228.txt` (confirmed — that file documents the commit under `## Changes`/
`Commit:` headings, no `Changed files:` line). `commit_verified_issue()` in `scripts/
dispatcher.sh` greps for that exact manifest line and fails immediately if absent, without ever
checking whether the work is already committed locally — so it never reaches the `git push`
step. Confirmed via `git log --oneline -3` and `git status --short --branch`: local `main` is
already at `53ceeea`, exactly **1 commit ahead of `origin/main`**, `git diff --cached` is empty
— the fix is fully done and committed, just never pushed. Per my role (plan/queue, don't
implement, never push to `main` myself), filed **#237** — `agent-todo`, no dependencies, ready
now — instructing a worker to add a fallback to `commit_verified_issue()`: when there's no
staged diff and local `main` has unpushed commits, push HEAD instead of failing the gate. Once
#237 lands, the dispatcher's next natural pass on #228 should push `53ceeea` and close #228
automatically with no further manual intervention. Flagging this as the top item for the next
pass to confirm.

**#226 closed (karen-verified `agent-done`) since last pass — scout entity, waypoint movement,
comms-bubble detection.** This was the rework attempt after the earlier diagonal tie-break FAIL;
it passed this time and is closed. Its closure already auto-promoted **#227** (`depends_on: #226`)
to `agent-todo` (confirmed via direct `gh issue view` — dispatcher had already applied this
before this pass ran, board matches). Re-checked the remaining backlog chain directly (not just
the board summary): **#231** (`depends_on: #224 ✅, #226 ✅, #228, #229`) stays `agent-backlog` —
#228 and #229 are still open (#228 in `agent-review` awaiting karen, #229 in `agent-todo`
unattempted). **#232** stays `agent-backlog` — depends on #227/#228/#229/#230/#231, none fully
closed yet. No untriaged issues, no `agent-triage` issues, no open `agent-question` issues, empty
inbox. Rolling 5h budget is at 88% of the $7.50 soft cap (was 91% last pass) — worker/karen
dispatch cadence still likely constrained by this; no lead action changes that. No relabeling or
issue creation needed this pass beyond confirming the above.

**Prior pass summary (unchanged, kept for continuity):** karen PASSED #225 last pass — closed. `state/verdict.txt` from the latest karen run: all
Done-when criteria met (tickPhase0Economy extends the #224 ResourceLedger; passive Energy drain
fixed at −1/tick floored at 0; harvester Biomass income at 2:1 conversion, max 2.0/tick, drawn
from real `world.getCell().producerBiomass` with cell floor and ledger cap; determinism verified;
73/73 tests pass). This was the 2nd attempt — the 1st and 2nd karen runs previously FAILed on a
broken test-fixture import, since fixed. Issue closed.

**#236 (orphaned uncommitted producer.ts diff) closed — resolved as a side effect.** The same
worker who fixed #225's test-fixture bug also reverted the stray, unrelated `producer.ts`
perf-refactor changes that had been sitting uncommitted for multiple lead passes (see
`worker_output_225.txt`). Confirmed `git diff -- src/simulation/producer.ts` is now empty — the
Done-when criteria were already satisfied with no separate commit needed. No worker dispatch
required; closed directly this pass.

**#228 and #229 promoted `agent-backlog` → `agent-todo`.** Both were gated only on #225 (#228
also needed #224, already closed) — now unblocked.

No inbox items, no untriaged issues, no open client questions this pass (#234 was answered and
closed last pass).

## What this project is

Project Origins: a persistent ecosystem simulation where players guide life evolution through
species creation and environmental stewardship. MVP shipped as a deterministic, local-first
evolution sandbox. Now pivoting the play loop on top of that same engine — see Phase, above.

## Active work

`agent-doing` is empty this pass. **#227** finally completed a worker attempt (9th try,
`MAX-TURNS-EXHAUSTED`, see "New this pass") and moved to `agent-review` — karen will verify it
automatically next tick under RULE 1. Its quality is unconfirmed (missing expected output
artifact, non-clean completion signal) — worth double-checking the verdict next pass. **#228**
still sits in `agent-blocked`: content-verified PASSED by karen 6 times total already, purely
waiting on #237's dispatcher fix — #237 has **not** closed yet, so #228 correctly stays
`agent-blocked` this pass (do not move it back to `agent-review` until #237 closes — see
"Blockers").

## Ready now (`agent-todo`)

| Issue | Title | Notes |
|-------|-------|-------|
| #229 | Biomass Harvester building lifecycle | Ready since 2 passes ago (#225 ✅ closed). Not yet attempted — was starved by #227 monopolizing dispatch during the outage window, which has now cleared (#227 out of the todo pool). Now oldest — will be dispatched next. |
| #230 | Equilibrium progress meter (Order/Chaos/Exploration/Replacement) | Ready since 2 passes ago; independent of the rest. Not yet attempted. |
| #237 | Fix dispatcher commit-gate false negative (unblocks #228) | Still highest-value fix once picked up — will resolve #228 automatically once it lands and closes. |
| #238 | Dispatcher: extend RULE 3 worker-crash cycling with the same fairness cooldown RULE 2 has | Fairness gap it fixes has already self-resolved for now (#227 out of the todo pool), but still good hygiene to land. |

## Awaiting verification (`agent-review`)

| Issue | Title | Notes |
|-------|-------|-------|
| #227 | Scout battery drain, discovery recording, sync/data-loss | Worker completed via `MAX-TURNS-EXHAUSTED` after 8 crashes; real diff exists (`scout.ts`, `scout.test.ts`) but `state/worker_output_227.txt` is missing. Karen will verify automatically (RULE 1). Check the verdict carefully next pass. |

## Blocked (`agent-blocked`) — needs manual/lead re-check, not a worker

| Issue | Title | Notes |
|-------|-------|-------|
| #228 | Localized Toxicity Spike crisis + Tier 1 templated response | Karen has PASSED content verification repeatedly across many runs, every time failing only the commit gate; commit `53ceeea` exists locally, unpushed. Pulled out of `agent-review` last pass to break a dispatcher livelock. Confirmed this pass: the livelock fix worked (zero further karen runs on #228 in the log). **Still do not dispatch a worker to this** — #237 has not closed yet, so it stays `agent-blocked` this pass too; relabel back to `agent-review` the pass after #237 closes, not before, not rework. |

## Backlog (waiting on dependencies)

### Phase 0 vertical slice (crisis-response pivot) — the active chain

| Issue | Title | Depends on | Status |
|-------|-------|------------|--------|
| #231 | Extend checkpointTimeline for Phase 0 state + validated command log | #224 ✅, #226 ✅, #228, #229 | waiting (#228, #229 open) |
| #232 | Integration harness wiring the full vertical slice | #225 ✅, #227, #228, #229, #230, #231 | waiting |
| #233 | **Verify: Phase 0 vertical slice** | #232 | waiting |

#227 promoted to `agent-todo` this pass (#226 closed). As #228/#229 close, watch for #231 (needs
#226 ✅, #228, #229) to auto-promote next pass.

### Pre-pivot roadmap — paused behind `#233 closes` (disposition confirmed by client in #234,
closed 2026-08-13)

30 issues, all carrying `depends_on: #233` (plus their original prerequisite chains where
applicable): #116 (sound/hearing ecology), #118 (toxicity enhancements epic), #119 (biomass
ecology), #120–#126 (soil/moisture, seasonal climate, plant competition, weather, causal God
Mode, catastrophes, disease epics), #155–#180 (Evolution Truth, Scalable Sim/Engine Scale,
Three.js Living World, Evolution Observatory, Knowledge/Scenarios epics and their children),
plus #200 (disaster events) and #223 (species composition chart) which depend on #233 directly.

Client-confirmed disposition (see SPEC.md "Legacy roadmap note" for the full rationale):
1. **Observatory epics (#159, #169–172) and Watch-panel items (#223) stay planned, not
   questionable.** The game's loop is Explore → Fix → Study → Watch — Watch is the win/
   completion condition, earned payoff, not the passive-viewer problem the pivot removes.
   Paused behind Phase 0 for sequencing only.
2. **Evolution Truth (#155, #165–168) and Engine Scale (#158, #173–176) pause, no parallel
   run** alongside Phase 0, as originally proposed.

No relabeling needed — board already matched this. Full re-evaluation of all 30 happens at the
Phase 0 checkpoint (#233 close).

## Verification (claimed vs verified)

- Phases 1–6 core build + the entire post-MVP beta-hardening backlog: all previously
  karen-verified / closed (see git history — dozens of merged PRs through #222).
- **#224** — karen-verified PASSED, committed, and closed. Unblocked #225/#226/#230.
- **#225** — karen-verified **FAILED twice**, then **PASSED** on the 3rd karen run this pass
  (test-fixture import bug fixed on the 2nd worker attempt). Closed.
- **#226** — karen-verified **FAILED** once (diagonal movement tie-break bug + undocumented
  field; see comment on #226), reworked, then **PASSED** on the 2nd karen run. Closed
  (`agent-done`). Unblocked #227.
- **#228** — worker done and committed (`53ceeea`), all 503 tests pass; **karen-verified PASSED
  repeatedly** (5 times this pass alone) but stuck on a dispatcher commit-gate bug, not a work
  gap — see #237. Now `agent-blocked` (moved out of `agent-review` this pass to stop the
  livelock — see "New this pass").
- **#236** — no formal karen pass needed; the orphaned diff it tracked was resolved as a side
  effect of the #225 rework (confirmed clean via `git diff`). Closed directly this pass.
- **#233** exists specifically to be the Phase 0 gate: karen will exercise the full loop
  (energy/biomass economy, scout waypoint+battery+sync, one crisis + Tier 1 response, building
  full lifecycle, checkpoint round-trip, command validation, equilibrium tracker) once #232
  lands. Nothing claims Phase 0 done yet.

## Blockers

- **#228 is blocked** (label `agent-blocked`, moved there last pass) on a dispatcher tooling bug
  (commit gate), not on missing work — see #237 (`agent-todo`, no deps, ready). Confirmed this
  pass that pulling it out of `agent-review` successfully broke the karen re-run livelock (zero
  further `VERIFY issue #228` lines in `logs/dispatcher.log` since). Still waiting on #237 to
  close before relabeling back.
- **Transient worker/karen/lead outage, 19:00–21:20 on 2026-08-13** (all three agent types hit
  `$0` cost / `duration_api_ms: 0` / rc=1 crashes in this window) exposed a real dispatcher
  fairness gap: RULE 3's worker-crash branch doesn't cooldown the crashed issue the way RULE 2's
  karen-FAIL branch does, so #227 (oldest ready issue) monopolized every dispatch attempt for
  2h10m while #229/#230/#237 got zero attempts. Not a true livelock (bounded by
  `max_worker_attempts=20`, only 7 used). Filed **#238** to close the gap for next time — still
  useful hygiene, though the immediate symptom self-resolved (see next item).
- **Resolved this pass:** #227 stopped crashing and completed (roughly — `MAX-TURNS-EXHAUSTED`,
  see "New this pass") at 22:21, moving out of the `agent-todo` pool into `agent-review`. This
  both confirms the outage cleared and removes the starvation pressure on #229/#230/#237 — no
  manual relabeling was needed. The new open question is #227's *quality*, not its dispatch
  fairness — see "Up next" #1.
- Otherwise none. #227, #229, #230, #237, #238 all sit in `agent-todo`, independent of each
  other, any order. No open client questions.

## Up next

1. **Check karen's verdict on #227 next pass.** Given the rough completion (max-turns-exhausted,
   missing `worker_output_227.txt`), confirm karen actually PASSED real, working
   battery-drain/discovery/sync behavior rather than a superficial pass — read
   `state/verdict.txt` and the diff, not just the label. If it FAILs, that's expected/fine
   (dispatcher will cycle it back to `agent-todo` for rework automatically); if it PASSes, sanity
   check the reasoning before trusting it fully, since this run's self-report is thinner than
   normal.
2. **Critical — do not skip:** once **#237 closes**, the very next lead pass must relabel **#228
   back from `agent-blocked` to `agent-review`** (`gh issue edit 228 --remove-label agent-blocked
   --add-label agent-review`) so the dispatcher's normal karen→commit-gate path finishes the
   close. Content is already verified PASSED 6 times — this should resolve in one more karen run
   with no rework. If #228 somehow FAILs on that re-run, that would be a genuine regression
   worth flagging, not expected. **#237 has not closed yet** as of this pass.
3. #229, #230, #237, #238 will get worked in the meantime (independent, any order, no lead
   action needed) — dispatcher picks oldest-number-first among non-cooldown candidates.
4. As #228/#229 close, #231 (needs #224 ✅, #226 ✅, #228, #229) should auto-promote to
   `agent-todo` on a subsequent planning pass — no lead action needed beyond the routine
   dependency check (`promote_backlog` runs automatically every lead pass).
5. Once **#232** (integration harness) lands and closes, #233 promotes and karen runs the full
   Phase 0 verification checklist.
6. Once **#233** PASSes, that's the trigger to do the full re-evaluation of the 30 paused
   pre-pivot-roadmap issues, per the disposition confirmed in #234 (Observatory stays planned;
   Evolution Truth/Engine Scale stay paused-not-parallel — see SPEC.md "Legacy roadmap note").
7. No open client questions this pass.

> Activity history: `logs/activity.log`; cost per run: `logs/usage.jsonl`.
> CLAUDE.md holds the full vision; SPEC.md is the living spec agents build against (see
> "Pivot: Crisis-Response Reframe" for the section actively being built).
