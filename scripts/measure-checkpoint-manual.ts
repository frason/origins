/**
 * DEPRECATED: Manual Checkpoint Size Estimation (Scratch Work)
 *
 * This file is DEPRECATED and should not be used.
 * Use scripts/measure-checkpoint-size.ts instead, which correctly imports
 * the real World, Creature, and captureCheckpoint production code.
 *
 * This hand-rolled reimplementation does NOT call real production code
 * and therefore does not provide valid measurement evidence.
 *
 * To measure checkpoint size, run:
 *   npm run measure:checkpoint
 */

console.error(
  'ERROR: measure-checkpoint-manual.ts is deprecated scratch work. Use measure-checkpoint-size.ts instead.'
);
